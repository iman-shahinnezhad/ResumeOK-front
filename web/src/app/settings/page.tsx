import Settings from '../../views/Settings';

export const metadata = {
  title: 'Account Settings & Preferences | ApplyDesk',
  description: 'Configure your notification preferences, account credentials, and system settings.'
};

export default function Page() {
  return <Settings />;
}
