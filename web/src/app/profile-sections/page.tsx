import ProfileSections from '../../views/ProfileSections';

export const metadata = {
  title: 'Custom Resume Sections | ApplyDesk',
  description: 'Customize education, skills, certifications, projects, and work history sections.'
};

export default function Page() {
  return <ProfileSections />;
}
